#!/usr/bin/python26
#encoding=utf-8

srcStr = "Hello hello world";
strs = srcStr.split(" ");

tmpSet = set()
sSet = set()

begin = strs[0]

for s in strs:
    # if tmpSet.
    pass





# Set<String> tmpSet = new TreeSet<>();
# Set<String> set = new TreeSet<>();
#
# for (String s : strs) {
#     if (!tmpSet.contains(s.toUpperCase())) {
#                 set.add(s);
#                 tmpSet.add(s.toUpperCase());
#             } else {
#                 tmpSet.add(s.toUpperCase());
#             }
#         }
#
#         StringBuffer stringBuffer = new StringBuffer("");
#
#         for (String s : set) {
#             stringBuffer.append(s).append(" ");
#         }
#
#         System.out.println(stringBuffer.toString().substring(0, stringBuffer.toString().length() - 1));


